version: "2"
services:
  nginx:
    build:
      context: .
    environment:
      - VIRTUAL_HOST=coresma-admin.com,www.coresma-admin.com
      - LETSENCRYPT_HOST=coresma-admin.com,www.coresma-admin.com
      - LETSENCRYPT_EMAIL=elderlopezfelix@gmail.com
    expose:
      - "80"
